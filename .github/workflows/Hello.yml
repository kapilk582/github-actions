# This is workflow name
name: Hello
# runs on trigger
on:
 workflow_dispatch: 
  #push: branches: [main]
    # paths defined as rules when change in yml file when it will run on push
   # paths:
     # - "**.yml"
#these are the jobs inside the workflow
jobs:

  # this is the name of the job
  greet:
  
  # this is the github runner named ubantu-latest
    runs-on: ubuntu-latest
    steps:
      - name: say hello to users
        run: echo "hello dosto"
      - name: say hello to users
        run: echo "hello $user"

  
